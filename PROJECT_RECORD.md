# AI 면접 평가 모델 개발 기록

## 1. 프로젝트 목표

이 프로젝트의 목표는 멘토-멘티 매칭 플랫폼에서 사용할 **AI 면접 평가 모델**을 만드는 것이다.

멘티가 모의 면접 질문에 답변하면 AI가 먼저 평가지를 생성하고, 멘토는 그 평가지를 검수하거나 수정한다. 이후 멘토가 수정한 데이터는 다시 모델 학습에 활용해서, 시간이 지날수록 모델이 우리 서비스의 멘토 평가 기준에 가까워지도록 만드는 구조를 목표로 한다.

최종적으로 AI가 출력해야 하는 형태는 단순 점수가 아니라 다음과 같은 JSON 평가 결과다.

```json
{
  "reasoning": "왜 이 점수인지에 대한 평가 이유",
  "overall_score": 7,
  "strengths": ["잘한 점 1", "잘한 점 2"],
  "improvements": ["개선점 1", "개선점 2"]
}
```

## 2. 왜 이런 모델이 필요한가

멘토가 모든 면접 답변을 처음부터 직접 평가하면 시간이 많이 든다. 특히 멘토-멘티 플랫폼에서는 멘토가 반복적으로 비슷한 질문과 답변을 검수해야 할 수 있다.

그래서 AI가 먼저 초안을 만들고, 멘토는 그 결과를 수정하는 방식이 더 현실적이라고 판단했다.

이 방식의 장점은 다음과 같다.

- 멘토의 반복 작업을 줄일 수 있다.
- 멘티는 더 빠르게 피드백을 받을 수 있다.
- 멘토가 수정한 데이터가 쌓이면 모델을 다시 개선할 수 있다.
- 단순 챗봇이 아니라 서비스 안에서 계속 좋아지는 평가 모델을 만들 수 있다.

## 3. 전체 개발 흐름

현재까지 진행한 전체 흐름은 다음과 같다.

```text
시드 질문 생성
  ↓
점수 구간별 QA 데이터 생성
  ↓
QA 데이터를 SFT 학습용 평가 JSON으로 변환
  ↓
데이터 검증
  ↓
train / val / test 분리
  ↓
SFT 학습 준비
  ↓
Colab T4에서 학습 예정
```

현재 로컬 Mac에서는 데이터 생성, 검증, 학습 스크립트 준비까지 완료했다. 실제 모델 학습은 Mac에 GPU가 없기 때문에 Google Colab의 무료 T4 GPU를 사용할 예정이다.

## 4. 데이터 구성

총 데이터 수는 **2,000개**로 구성했다.

직군별 분포는 다음과 같다.

| 직군 | 개수 |
|---|---:|
| 백엔드 | 600 |
| 데이터·AI/ML | 600 |
| 프론트엔드 | 400 |
| 인프라·DevOps | 400 |

점수 구간은 네 구간으로 나누고 균등하게 맞췄다.

| 점수 구간 | 개수 |
|---|---:|
| 1-3 | 500 |
| 4-6 | 500 |
| 7-8 | 500 |
| 9-10 | 500 |

점수 구간을 균등하게 만든 이유는 모델이 중간 점수에만 치우치지 않도록 하기 위해서다. 실제 LLM으로 데이터를 만들면 보통 5-8점 정도의 무난한 답변이 많이 나오기 때문에, 처음부터 점수 구간을 나누어 생성하는 방식이 필요했다.

## 5. 점수 기준을 나눈 이유

면접 평가는 단순히 맞다/틀리다로 끝나지 않는다. 같은 질문에 대해서도 지원자의 답변 수준이 크게 다를 수 있다.

그래서 다음과 같이 점수 구간을 나누었다.

### 1-3점

핵심 개념을 잘못 이해하거나, 다른 개념과 혼동하는 답변이다.

예시:

```text
TCP는 빠른 전송을 위해 연결 없이 데이터를 보내는 방식이고,
handshake는 중간에 패킷을 압축하는 과정이라고 알고 있습니다.
```

이 답변은 TCP와 UDP를 혼동하고 있으므로 낮은 점수를 준다.

### 4-6점

기본 방향은 조금 알지만 설명이 피상적인 답변이다.

예시:

```text
인덱스는 조회를 빠르게 해주는 기능이라고 알고 있습니다.
다만 어떤 컬럼에 걸어야 하는지, 실행 계획을 어떻게 확인하는지는 잘 모르겠습니다.
```

완전히 틀린 것은 아니지만, 면접에서 깊게 물어보면 답변이 부족할 가능성이 높다.

### 7-8점

핵심 개념을 정확히 설명하고, 주의점도 일부 언급하는 답변이다.

예시:

```text
인덱스는 테이블 전체를 스캔하지 않고 조건에 맞는 데이터를 빠르게 찾기 위한 자료구조입니다.
다만 쓰기 작업에서는 인덱스 갱신 비용이 생길 수 있습니다.
```

면접관이 기본 실무 역량을 납득할 수 있는 수준이다.

### 9-10점

개념, 동작 원리, 트레이드오프, 운영 관점까지 함께 설명하는 답변이다.

예시:

```text
인덱스는 조회 성능을 높이지만 INSERT, UPDATE 시 갱신 비용이 생깁니다.
복합 인덱스는 컬럼 순서가 중요하고, 실제 쿼리 패턴과 실행 계획을 보고 설계해야 합니다.
운영에서는 응답 시간과 DB 부하 지표를 함께 확인해야 합니다.
```

단순히 개념을 아는 수준을 넘어서 실제 서비스에서 어떻게 판단할지까지 말하는 답변이다.

## 6. SFT를 사용하는 이유

SFT는 Supervised Fine-Tuning의 줄임말이다. 쉽게 말하면, 모델에게 "이런 입력이 들어오면 이런 형식으로 답하라"고 예시를 보여주며 학습시키는 방식이다.

이 프로젝트에서는 모델이 단순히 자유롭게 답변하는 것이 아니라, 항상 정해진 JSON 형식으로 평가 결과를 출력해야 한다.

그래서 먼저 SFT를 통해 다음을 학습시키려고 한다.

- 면접 질문과 지원자 답변을 읽는 법
- 답변 수준에 맞는 점수 감각
- `reasoning`, `overall_score`, `strengths`, `improvements` 형식
- 한국어 면접 평가 문체

SFT를 먼저 하는 이유는, 아무 학습도 하지 않은 모델에게 바로 멘토 스타일을 맞추라고 하기 어렵기 때문이다. 먼저 기본 평가자 역할을 학습시킨 뒤, 나중에 멘토 수정 데이터를 활용해 더 정렬하는 흐름이 안정적이라고 판단했다.

## 7. DPO는 나중에 사용하는 이유

DPO는 Direct Preference Optimization의 줄임말이다. 쉽게 말하면, 모델이 만든 답변과 사람이 더 좋게 고친 답변을 비교해서 "사람이 선호한 답변" 쪽으로 모델을 맞추는 방식이다.

이 서비스에서는 다음과 같은 데이터가 자연스럽게 생긴다.

```json
{
  "prompt": "면접 질문 + 지원자 답변",
  "rejected": "AI가 처음 만든 평가",
  "chosen": "멘토가 수정한 평가"
}
```

여기서 `chosen`은 멘토가 고친 더 좋은 평가이고, `rejected`는 AI가 처음 만든 평가다.

하지만 DPO는 운영 데이터가 어느 정도 쌓인 뒤에 의미가 있다. 현재는 아직 실제 멘토 수정 데이터가 없기 때문에, 먼저 합성 데이터로 SFT를 진행하고 이후 서비스에서 멘토 수정 데이터를 모아 DPO를 적용하는 방향이 적절하다.

## 8. 현재 만든 파일과 역할

### 데이터 생성 관련

| 파일 | 역할 |
|---|---|
| `data/seeds/seed_questions.jsonl` | 직군별 면접 질문 시드 |
| `data/raw/qa_responses/*.json` | 점수 구간별 질문-답변 원본 |
| `data/raw/qa_pairs.jsonl` | 모든 QA를 하나로 병합한 파일 |
| `data/processed/sft_dataset.jsonl` | SFT 학습용 최종 데이터 |
| `data/splits/train.jsonl` | 학습 데이터 |
| `data/splits/val.jsonl` | 검증 데이터 |
| `data/splits/test.jsonl` | 테스트 데이터 |

### 스크립트

| 파일 | 역할 |
|---|---|
| `scripts/01_make_seed_questions.py` | 시드 질문 생성 |
| `scripts/02_generate_qa_pairs.py` | QA 프롬프트 생성 및 응답 병합 |
| `scripts/03_refine_qa_responses.py` | QA 응답 보정 및 재생성 |
| `scripts/03_generate_evaluations.py` | QA를 SFT 평가 JSON으로 변환 |
| `scripts/04_validate_dataset.py` | 데이터셋 검증 |
| `scripts/05_split_dataset.py` | train/val/test 분리 |
| `scripts/06_train_sft.py` | SFT 학습 스크립트 |

## 9. 데이터 검증에서 고려한 점

학습 데이터는 모델 성능에 직접 영향을 준다. 그래서 단순히 파일이 존재하는지만 확인하지 않고 다음 항목을 검증했다.

### 1. JSON 형식

모델 학습에 넣을 데이터는 JSONL 형식이다. 한 줄이라도 JSON이 깨지면 학습 중 오류가 날 수 있다.

### 2. 필수 필드

각 샘플은 반드시 다음 필드를 가져야 한다.

```text
instruction
input.question
input.answer
output.reasoning
output.overall_score
output.strengths
output.improvements
```

### 3. 점수 일관성

`overall_score`가 1-10 범위 안에 있어야 하고, `score_band`와도 맞아야 한다.

예를 들어 `overall_score`가 8인데 `score_band`가 `1-3`이면 잘못된 데이터다.

### 4. 분포

직군과 점수 구간이 한쪽으로 치우치지 않도록 확인했다.

현재 결과:

```text
총 2,000개
백엔드 600
데이터·AI/ML 600
프론트엔드 400
인프라·DevOps 400

1-3 / 4-6 / 7-8 / 9-10 각 500개
```

### 5. 중복과 다양성

처음에는 reasoning과 strengths/improvements가 너무 반복되는 문제가 있었다. 이 경우 모델이 점수와 무관하게 비슷한 평가 문장을 출력할 위험이 있다.

해결 방법:

- 점수별 reasoning 문장을 더 세분화했다.
- 질문 주제와 metadata를 평가 문장에 반영했다.
- `topic_tag`, `question_type`, `score_band`를 활용했다.
- 7-8점 답변과 9-10점 답변이 같은 문장이 되지 않도록 분리했다.

최종 확인 결과:

```text
7-8 답변과 9-10 답변 overlap: 0개
reasoning unique: 945개
strengths unique: 1169개
improvements unique: 1143개
```

## 10. 발생한 문제와 해결

### 문제 1. 점수대별 답변이 충분히 구분되지 않음

초기에는 7-8점 답변과 9-10점 답변이 거의 비슷했다. 9-10점은 단순히 7-8점 답변 뒤에 좋은 문장을 조금 붙인 정도였다.

해결:

- 9-10점 답변에는 운영 지표, 실패 시나리오, 대안 비교, 엣지 케이스를 더 넣었다.
- 답변 길이도 점수대별로 자연스럽게 차이가 나게 했다.

최종 평균 길이:

```text
1-3 평균 120.6자
4-6 평균 163.2자
7-8 평균 217.6자
9-10 평균 328.1자
```

### 문제 2. reasoning이 반복됨

초기 평가지에서는 7점과 9점의 reasoning 구조가 거의 같았다.

해결:

- 점수별 reasoning 템플릿을 세분화했다.
- 질문 유형과 topic_tag를 reasoning에 반영했다.
- 답변 안의 단서에 따라 다른 평가 문장을 생성하도록 했다.

### 문제 3. metadata가 부족하면 추적이 어려움

학습 데이터가 많아질수록 어떤 기준으로 만든 데이터인지 추적하기 어려워진다.

해결:

metadata에 다음 정보를 넣었다.

```json
{
  "source_qa_id": "qa_00001",
  "category": "백엔드",
  "question_type": "CS 기초",
  "score_band": "1-3",
  "topic_tag": "network/tcp",
  "answer_score": 1,
  "label_source": "synthetic_rule_based",
  "needs_mentor_review": true,
  "generated_by": "rule_based_eval_v2",
  "reviewed": false,
  "version": "v2"
}
```

특히 `needs_mentor_review: true`는 이 데이터가 아직 멘토 검수를 거치지 않은 합성 데이터라는 의미다.

### 문제 4. Mac에서는 학습이 어려움

현재 개발 환경은 Mac이고, 별도 GPU가 없다. Qwen2.5-7B 모델을 LoRA로 학습하려면 GPU 메모리가 필요하다.

해결:

- Mac에서는 데이터 준비, 검증, ChatML dry-run까지만 진행한다.
- 실제 학습은 Google Colab T4 GPU에서 실행한다.
- 학습 결과는 Google Drive에 저장한다.

## 11. 왜 GitHub와 Google Drive를 나누어 쓰는가

GitHub에는 코드, 설정, 작은 학습 데이터를 올린다.

```text
scripts/
configs/
data/
CLAUDE.md
COLAB_GUIDE.md
pyproject.toml
```

Google Drive에는 학습 결과를 저장한다.

```text
models/lora_adapters/
checkpoints/
```

이렇게 나누는 이유는 모델 파일은 용량이 크고 GitHub에 올리기에 적합하지 않기 때문이다. 반면 코드와 데이터는 GitHub에 있어야 Colab에서 `git clone`으로 쉽게 가져올 수 있다.

## 12. Colab에서 학습하는 방식

Colab에서는 다음 순서로 진행한다.

```text
1. T4 GPU 런타임 설정
2. Google Drive 마운트
3. GitHub repo clone
4. 의존성 설치
5. 데이터 검증
6. ChatML dry-run
7. 작은 샘플 학습
8. 전체 학습
9. LoRA adapter를 Drive에 저장
```

먼저 작은 샘플로 학습하는 이유는 전체 학습 전에 다음을 확인하기 위해서다.

- GPU 메모리가 부족하지 않은지
- 학습 스크립트가 정상 동작하는지
- JSON 출력 형식이 잘 학습되는지
- checkpoint가 Drive에 저장되는지

## 13. 현재 완료된 단계

현재까지 완료된 단계는 다음과 같다.

```text
[x] 시드 질문 준비
[x] QA 원본 데이터 2,000개 생성
[x] SFT 평가 JSON 데이터 생성
[x] 데이터 검증
[x] train/val/test 분리
[x] SFT 학습 스크립트 작성
[x] Colab 실행 가이드 작성
[ ] Colab T4에서 작은 샘플 학습
[ ] 전체 SFT 학습
[ ] SFT 모델 평가
[ ] FastAPI 추론 서버 연결
[ ] 멘토 수정 데이터 기반 DPO
```

## 14. 다음 할 일

가장 먼저 해야 할 일은 Colab에서 작은 샘플 학습을 돌리는 것이다.

작은 샘플 학습이 성공하면 전체 2,000개 데이터로 SFT를 진행한다.

그 다음에는 학습 전 모델과 학습 후 모델을 비교해야 한다.

비교 지표는 다음과 같다.

- JSON 파싱 성공률
- 점수 MAE
- 점수 구간 정확도
- 피드백 품질 샘플 확인

최종적으로는 FastAPI 추론 서버를 만들어 백엔드/프론트엔드와 연결할 예정이다.

## 15. 느낀 점

이번 단계에서 가장 중요했던 것은 모델 학습 코드보다 **데이터 품질과 구조**였다.

처음에는 단순히 질문과 답변을 많이 만들면 된다고 생각할 수 있지만, 실제로는 점수 구간별 차이, 평가 문장의 다양성, metadata 관리가 중요했다.

특히 SFT 모델은 주어진 데이터를 그대로 따라 배우기 때문에, 데이터 안에 반복 패턴이 많으면 모델도 반복적인 평가를 만들 가능성이 높다.

그래서 이번에는 단순히 데이터를 생성하는 것에서 끝내지 않고, 검증 스크립트를 만들어 점수 분포, 중복률, 필드 구조를 확인했다.

이후 멘토가 실제로 수정한 데이터를 모으면, 지금 만든 합성 데이터보다 더 서비스에 맞는 평가 모델로 발전시킬 수 있을 것이다.
